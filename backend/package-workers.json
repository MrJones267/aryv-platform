{
  "name": "aryv-workers-api",
  "version": "1.0.0",
  "description": "ARYV API backend for Cloudflare Workers",
  "main": "workers-entry.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler publish",
    "dev": "wrangler dev",
    "test": "wrangler dev --local"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  },
  "engines": {
    "node": ">=16"
  }
}